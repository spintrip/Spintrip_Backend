const db = require('./Models'); (async () => { try { await db.sequelize.authenticate(); await db.sequelize.query('ALTER TABLE "HostAdditionals" ADD COLUMN IF NOT EXISTS "pan" VARCHAR(255);'); console.log('✅ DATABASE FIXED: Host pan photo column added successfully.'); } catch(e) { console.error('❌ FIX FAILED:', e.message); } finally { await db.sequelize.close(); process.exit(0); } })()